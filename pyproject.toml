[project]
name = "python-final-project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "조성아", email = "agnus.oj14@gmail.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
python-final-project = "python_final_project:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
